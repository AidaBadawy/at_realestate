enum StatusEnum {
  busy,
  idle,
}

enum PaymentTypeEnum {
  tenant,
  landlord,
}

enum AlignmentEnum {
  right,
  left,
}

enum MessageType {
  text,
  image,
  audio,
  video,
  log,
}

enum NotificationType {
  mention,
  comment,
  request,
}
